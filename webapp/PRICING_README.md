# Система ценообразования для AdDesigner Bot

## Обзор

Профессиональная система тарифов с гибким ценообразованием и динамическим расчётом стоимости.

## Структура файлов

- `pricing_config.json` - Конфигурация тарифов, услуг и цен
- `pricing_calculator.js` - Калькулятор стоимости
- `unified_order.html` - Страница оформления заказа

## Тарифы

### Basic (Базовый)
- **Цена**: 500₽ / 6$ / 40¥
- **Включено**:
  - 1 канал размещения
  - 1 объявление
  - Базовая аналитика
  - Стандартная поддержка

### Standard (Стандарт)
- **Цена**: 1200₽ / 14$ / 95¥
- **Включено**:
  - До 3 каналов
  - До 3 объявлений
  - Базовая аналитика
  - Приоритетная поддержка
  - ✅ Закрепление на 3 дня (бесплатно!)
  - ✅ Отложенная публикация (бесплатно!)

### Premium (Премиум)
- **Цена**: 2500₽ / 30$ / 200¥
- **Включено**:
  - Неограниченно каналов
  - Неограниченно объявлений
  - Расширенная аналитика
  - VIP поддержка 24/7
  - ✅ Закрепление на 7 дней (бесплатно!)
  - ✅ Отложенная публикация (бесплатно!)
  - ✅ Комбинированный формат (бесплатно!)
  - ✅ Размещение 60+ дней (бесплатно!)
  - ✅ Автопродвижение (бесплатно!)

## Дополнительные услуги

### Закрепление
- **1 день**: 100₽ / 1$ / 7¥
- **3 дня**: 250₽ / 3$ / 20¥ (входит в Standard и Premium)
- **7 дней**: 500₽ / 6$ / 40¥ (входит в Premium)

### Форматы
- **Видео формат**: 200₽ / 2$ / 15¥
- **Комбинированный**: 300₽ / 4$ / 25¥ (входит в Premium)

### Прочее
- **Отложенная публикация**: 150₽ / 2$ / 12¥ (входит в Standard и Premium)
- **Размещение 60+ дней**: 400₽ / 5$ / 30¥ (входит в Premium)
- **Автопродвижение**: 800₽ / 10$ / 65¥ (входит в Premium)

## Как изменить цены

### 1. Изменить базовые тарифы

Отредактируйте `pricing_config.json`:

```json
{
  "tariffs": {
    "basic": {
      "prices": {
        "RUB": 600,  // Было 500
        "USD": 7,    // Было 6
        "CNY": 45    // Было 40
      }
    }
  }
}
```

### 2. Добавить новую услугу

```json
{
  "additional_services": {
    "my_new_service": {
      "name": {
        "ru": "Название услуги",
        "en": "Service name",
        "zh-tw": "服務名稱"
      },
      "prices": {
        "RUB": 300,
        "USD": 4,
        "CNY": 25
      },
      "icon": "⭐",
      "included_in": ["premium"]  // В какие тарифы входит бесплатно
    }
  }
}
```

### 3. Изменить, что входит в тариф

```json
{
  "tariffs": {
    "standard": {
      "included_features": [
        "multiple_channels",
        "pinning_3days",
        "scheduling",
        "video_format"  // Добавили видео в Standard
      ]
    }
  }
}
```

## Логика расчёта цен

### Базовый принцип

```
Итоговая цена = Цена тарифа + Сумма доп. услуг (не входящих в тариф)
```

### Примеры

#### Пример 1: Basic + закрепление на 7 дней
```
500₽ (тариф) + 500₽ (закрепление) = 1000₽
```

#### Пример 2: Standard + закрепление на 7 дней
```
1200₽ (тариф) + 500₽ (закрепление 7 дней) = 1700₽
```
Закрепление на 3 дня входит бесплатно, но на 7 - доплата

#### Пример 3: Premium + закрепление на 7 дней
```
2500₽ (тариф) + 0₽ (входит в тариф) = 2500₽
```

#### Пример 4: Basic + видео + комбинированный + автопродвижение
```
500₽ + 200₽ + 300₽ + 800₽ = 1800₽
```
Но если выбрать Premium:
```
2500₽ (всё включено) = 2500₽ - ВЫГОДНЕЕ!
```

## Подсказки для пользователя

Система автоматически показывает:

✅ **"Входит в ваш тариф бесплатно"** - если услуга уже включена

💡 **"Входит бесплатно в: Premium"** - если можно сэкономить, выбрав другой тариф

🔔 **"Доплата +500₽"** - если нужно доплатить за услугу

## API калькулятора

### Инициализация

```javascript
const calculator = new PricingCalculator(pricingConfig);
```

### Основные методы

```javascript
// Выбрать тариф
calculator.setTariff('standard');

// Установить валюту
calculator.setCurrency('USD');

// Добавить услугу
calculator.addService('pinning_7days');

// Проверить, входит ли услуга в тариф
calculator.isIncludedInTariff('scheduling'); // true для Standard

// Получить цену услуги (с учётом тарифа)
calculator.getServicePrice('pinning_7days'); // 500₽ для Basic, 0₽ для Premium

// Рассчитать итоговую стоимость
calculator.calculateTotal(); // 1200₽ + 500₽ = 1700₽

// Получить подсказку
calculator.getServiceHint('auto_promotion', 'ru');
// "💡 Входит бесплатно в: Премиум"

// Получить полную детализацию заказа
const details = calculator.getOrderDetails('ru');
console.log(details);
/*
{
  tariff: 'standard',
  tariffName: 'Стандарт',
  tariffPrice: 1200,
  services: [
    {
      id: 'pinning_7days',
      name: 'Закрепление на 7 дней',
      price: 500,
      isIncluded: false,
      icon: '📍'
    }
  ],
  total: 1700
}
*/
```

## Интеграция в unified_order.html

### 1. Подключить скрипты

```html
<script src="pricing_calculator.js"></script>
```

### 2. Загрузить конфигурацию

```javascript
async function loadPricing() {
    const response = await fetch('pricing_config.json');
    const config = await response.json();
    const calculator = new PricingCalculator(config);
    return calculator;
}
```

### 3. Обновить цены динамически

```javascript
// При выборе тарифа
function selectTariff(tariffId) {
    calculator.setTariff(tariffId);
    updateAllPrices();
}

// При выборе закрепления
function selectPinning(days) {
    const serviceId = `pinning_${days}days`;
    const price = calculator.getServicePrice(serviceId);
    const hint = calculator.getServiceHint(serviceId, userLang);
    
    // Показать цену и подсказку
    document.getElementById('pinPrice').textContent = 
        price > 0 ? `+${calculator.formatPrice(price)}` : 'FREE';
    document.getElementById('pinHint').textContent = hint;
}
```

## Бизнес-логика

### Почему такая структура?

1. **Градация тарифов** - мотивирует выбрать Premium для экономии
2. **Прозрачность** - пользователь видит все цены и подсказки
3. **Гибкость** - легко менять цены в JSON без кода
4. **Upsell** - показываем, что Premium выгоднее при многих доп. услугах

### Стратегия ценообразования

- Basic - "пробный" тариф для новых клиентов
- Standard - оптимальный выбор с популярными фичами
- Premium - максимальная выгода при активном использовании

### Психология подсказок

- ✅ Зелёная галочка - радость от бесплатного
- 💡 Лампочка - намёк на возможность сэкономить
- 🔔 Колокольчик - внимание к доплате

## Тестирование цен

```bash
# Пример тестовых сценариев

# 1. Минимальный заказ
Basic + без допов = 500₽

# 2. Средний заказ
Standard + закрепление 7д = 1700₽

# 3. Максимальный заказ
Premium + все фичи = 2500₽ (всё включено!)

# 4. Неоптимальный выбор (показать подсказку!)
Basic + видео + комбо + авто = 1800₽
Подсказка: "Выберите Premium за 2500₽ и получите всё это бесплатно!"
```

## Обновление конфигурации

После изменения `pricing_config.json`:

1. Сохранить файл
2. Закоммитить: `git commit -m "Update pricing"`
3. Задеплоить на gh-pages
4. Обновления применятся автоматически

Не требуется изменение HTML/JS - вся логика читается из JSON!
